<h2>Lessons</h2>

<ul>
  {{#each controller}}
  	{{#if name}}
    	<li class="course-link">{{#link-to "lesson" this}}{{name}}{{/link-to}}</li>
  	{{/if}}
  {{else}}
    <li>There are no lessons.</li>
  {{/each}}
</ul>